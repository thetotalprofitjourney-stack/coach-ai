// Placeholder de seed. El Paso 1 no necesita datos de seed; este fichero queda
// registrado en package.json para que `prisma db seed` no falle y para tener
// un punto de entrada claro cuando se necesite en pasos posteriores.

async function main() {
  console.log('[seed] Paso 1: nothing to seed yet.');
}

main();
